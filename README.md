# Array Tasks Library (C++)

Учебная библиотека функций для работы с массивами на C++.  
Предназначена для школьных и университетских лабораторных работ, демонстрации алгоритмов и обучения основам обработки данных.

Библиотека покрывает широкий спектр задач: от базовых операций до продвинутых алгоритмов.

---

## 📌 Особенности

- Реализация **базовых операций**: сумма, максимум, абсолютные значения, разворот массива, подсчет элементов больше среднего.
- **Средний уровень**: проверка сортировки, подсчет уникальных элементов, циклический сдвиг массива.
- **Продвинутый уровень**: пузырьковая сортировка, удаление дубликатов, слияние массивов, поиск пар с заданной суммой, подсчет троек с суммой 0.
- **Вывод массива** на экран.
- Указание **временной сложности алгоритмов** прямо в коде.
- Интерактивное **меню с русской локализацией**.
- Поддержка UTF-8 для корректного отображения русских символов.
- Разделение на `.h` и `.cpp` файлы для удобной интеграции.

---

## 🛠 Требования к системе

Для корректной работы библиотеки и примера с меню необходимы следующие условия:

### Минимальные требования
- **Компилятор C++17 или выше**  
  Поддержка стандартов C++17/C++20 для корректной работы `std::vector`, `size_t`, `std::swap` и других стандартных функций.
- **Операционная система**: Windows
- **Консоль с поддержкой UTF-8** для корректного отображения русских символов.

### Рекомендации для Windows
- Установите кодовую страницу UTF-8 в консоли:
  ```cpp
  #include <windows.h>
  SetConsoleCP(65001);
  SetConsoleOutputCP(65001);


## 📁 Структура проекта


⚠ Проблемы

Отображение русских символов в консоли:
Иногда русские символы могут отображаться некорректно из-за настроек кодовой страницы.

Windows: использовать SetConsoleCP(65001) и SetConsoleOutputCP(65001).

Linux/macOS: убедиться, что терминал использует кодировку UTF-8.

В некоторых средах разработки (например, старые версии Visual Studio) возможны проблемы с кодировкой исходного файла.

🚀 Планы на будущее

Поддержка строк и текстовых данных (std::string) и их обработка.

Работа с двумерными массивами (матрицами).

Добавление дополнительных алгоритмов сортировки и сравнение их эффективности.

Использование STL контейнеров (std::vector, std::set, std::map) для оптимизации и упрощения кода.

Разработка интерактивных тестов и примеров использования для каждой функции.
