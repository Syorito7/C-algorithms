# 📚 C++ Learning Tasks Library

Учебная библиотека для изучения работы с массивами, динамической памятью, строками, матрицами, структурами данных и алгоритмами на C++.

Подходит для:
- школьных и университетских лабораторных работ
- самостоятельного изучения C++
- демонстрации алгоритмов на уроках и занятиях

---

## 🔧 Возможности библиотеки

### **🟢 Базовый уровень**
Работа с одномерными массивами:
- сумма, максимум, поиск элементов
- перевод значений в абсолютные
- разворот массива
- подсчёт элементов больше среднего

### **🟡 Средний уровень**
- проверка, отсортирован ли массив
- подсчёт количества уникальных значений
- циклический сдвиг массива

### **🔴 Продвинутый уровень**
- пузырьковая сортировка
- удаление дубликатов
- слияние отсортированных массивов
- поиск пар с заданной суммой
- поиск троек с суммой `0`

---

### **🟣 Работа с указателями**

В этом разделе изучаются основы работы с указателями: передача по адресу, разыменование и изменение значений через указатели.
Задачи:

Присваивание значения через указатель – изменить значение переменной через указатель.

Обратный вывод массива – пройти по массиву с конца к началу через указатели.

Увеличение числа через указатель – создать функцию, которая увеличивает значение переменной.

Обмен значений двух переменных – поменять значения двух переменных через функцию с указателями.

---

## 🔤 Работа со строками
- анализ `std::string` (поиск, подсчёт символов, сравнение)
- работа с UTF-8
- задачи на обработку текста

---

## 🔢 Работа с матрицами (`Matrix`)
- вывод матрицы
- сумма элементов
- максимум в матрице
- суммы по строкам / столбцам
- сумма главной диагонали
- разница диагоналей
- сортировка строк по возрастанию
- поворот квадратной матрицы на 90°
- **динамические двумерные массивы**
- **транспонирование**

---

## 💾 Динамическая память
Включены задачи:
- `pushBack` — добавление элемента вручную (без vector)
- `removeElement` — удаление элемента с сдвигом
- создание и удаление 2D массива через `new[]`
- меню для работы с динамическими массивами

## Сложность алгоритмов

## ⏱ Временная и пространственная сложность операций

| Модуль         | Операция                                 | Время (Big-O)  | Память (Big-O) | Примечание                                        |
| -------------- | ---------------------------------------- | -------------- | -------------- | ------------------------------------------------- |
| **1D массивы** | Добавление элемента вручную (`pushBack`) | O(n)           | O(n)           | Копирование всего массива, создаётся новый массив |
| **1D массивы** | Удаление элемента (`removeElement`)      | O(n)           | O(1)           | Сдвиг элементов выполняется на месте              |
| **1D массивы** | Изменение элемента по индексу            | O(1)           | O(1)           | Прямой доступ                                     |
| **1D массивы** | Вывод массива                            | O(n)           | O(1)           | Проход по всем элементам, память не увеличивается |
| **1D массивы** | Сумма элементов                          | O(n)           | O(1)           | Линейный проход                                   |
| **1D массивы** | Максимум                                 | O(n)           | O(1)           | Линейный проход                                   |
| **1D массивы** | Разворот массива                         | O(n)           | O(1)           | Обмен элементов на месте                          |
| **1D массивы** | Проверка сортировки                      | O(n)           | O(1)           | Однократный проход                                |
| **1D массивы** | Подсчёт уникальных элементов             | O(n²)          | O(1)           | Простая реализация без дополнительной памяти      |
| **1D массивы** | Циклический сдвиг                        | O(n)           | O(n)           | Если используется временный массив                |
| **1D массивы** | Пузырьковая сортировка                   | O(n²)          | O(1)           | Сортировка на месте                               |
| **1D массивы** | Удаление дубликатов                      | O(n²)          | O(1)           | Простая проверка каждого элемента                 |
| **1D массивы** | Слияние двух массивов                    | O(n + m)       | O(n + m)       | Создаётся новый массив для результата             |
| **1D массивы** | Поиск пар с суммой k                     | O(n²)          | O(1)           | Двойной цикл                                      |
| **1D массивы** | Подсчёт троек с суммой 0                 | O(n³)          | O(1)           | Тройной цикл                                      |
| **Строки**     | Подсчёт символов                         | O(n)           | O(1)           | n — длина строки                                  |
| **Строки**     | Поиск подстроки                          | O(n*m)         | O(1)           | n — длина строки, m — длина подстроки             |
| **Матрицы**    | Создание 2D массива                      | O(rows * cols) | O(rows * cols) | Инициализация всех элементов                      |
| **Матрицы**    | Транспонирование                         | O(rows * cols) | O(rows * cols) | Создаётся новая матрица                           |
| **Матрицы**    | Вывод матрицы                            | O(rows * cols) | O(1)           | Проход по всем элементам                          |
| **Матрицы**    | Сумма элементов                          | O(rows * cols) | O(1)           | Линейный проход                                   |
| **Матрицы**    | Суммы по строкам/столбцам                | O(rows * cols) | O(rows + cols) | Если хранить суммы отдельно                       |
| **Матрицы**    | Максимум                                 | O(rows * cols) | O(1)           | Линейный проход                                   |
| **Матрицы**    | Поворот 90°                              | O(n²)          | O(n²)          | Создаётся новая квадратная матрица                |
| **Указатели**  | Увеличение числа через указатель         | O(1)           | O(1)           | Простое изменение значения                        |
| **Указатели**  | Обмен значений двух переменных           | O(1)           | O(1)           | Меняем через адреса                               |
| **Указатели**  | Присваивание через указатель             | O(1)           | O(1)           | Прямое изменение значения переменной              |
| **Указатели**  | Обратный вывод массива через указатель   | O(n)           | O(1)           | Проход по массиву с конца к началу                |




```cpp
dynamic::menu();


